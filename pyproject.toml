[project]
name = "ai-migration-tool"
version = "0.1.0"
description = "AI-driven migration tool from legacy SQL to Snowflake-style warehouse"
requires-python = ">=3.10"
author = "Amos Bunde"
email = "iambunde@proton.me"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]